import { Grid, Typography } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Grid>
        <Typography> Hello</Typography>
        <Typography> Hello</Typography>
        <Typography> Hello</Typography>
        <Typography> Hello</Typography>
        <Typography> Hello</Typography>
        <Typography> Hello</Typography>
        <Typography> Hello</Typography>
        <Typography> Hello</Typography>
        <Typography> Hello</Typography>
        <Typography> Hello</Typography>
        <Typography> Hello</Typography>
      </Grid>
    </div>
  );
};

export default Home();
