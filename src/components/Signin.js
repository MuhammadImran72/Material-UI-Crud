import { Grid, Typography } from "@mui/material";

const Signin = () => {
  return (
    <Grid>
      <Typography>Login </Typography>
      <Typography>Login </Typography>
      <Typography>Login </Typography>
      <Typography>Login </Typography>
      <Typography>Login </Typography>
      <Typography>Login </Typography>
      <Typography>Login </Typography>
    </Grid>
  );
};

export default Signin();
